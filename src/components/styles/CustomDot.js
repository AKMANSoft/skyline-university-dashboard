'use client'
import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const CustomDot = styled(Box)(({ theme }) => ({
    width: '7.6px',
    minWidth: '7.6px',
    borderRadius: '50%'
}));